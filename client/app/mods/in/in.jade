//    in.jade is a top level parent view, including an "add button" for
//    a new item and a lookup/search field.
//    Nested views include a lookup table and an edit form under that view.
//    The toaster container for this feature set is within this parent view.
//    in the search field to get a full lookup table. They can currently click the
//    search button to lookup an inventory item by an id entered into the search field.
extends ../../app.jade
block container
  .container
    .row
      .col-md-6
        h3
          | Receive Parts&nbsp;
          button.btn.btn-success.btn-circle#newitem(ng-click="vm.showEditForm('0')", type='button')
            span.glyphicon.glyphicon-plus(title='New Item')
        form(ng-submit='vm.onSubmit()', novalidate='')
          formly-form(model='vm.formlyModel', fields='vm.formlyFields', form='formlyForm', options='vm.formlyOptions')
            button.btn.btn-primary.submit-button(type='submit', ng-disabled='formlyForm.$invalid') Submit
            |&nbsp;
            button.btn.btn-default(type='button', ng-click='vm.formlyOptions.resetModel()') Reset
    .row
      div.col-md-6(ui-view='', autoscroll='true')
